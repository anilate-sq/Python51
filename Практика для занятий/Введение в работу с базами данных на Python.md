# Введение в работу с базами данных на Python

**Задание 1. Подключение**

1. Установить psycopg2-binary/psycopg2
2. Создать функицию connect_db(), для подключения к базе данных
3. Выполнить проверку подключения

**Задание 2. READ**

Реализовать следующие функции
```python
def get_movies_by_year():
    # Все фильмы за определенный год
    
def get_longest_movies(top_n=5):
    # Вывести топ-N самых длительных фильмов

def get_movie_for_genre:
    # Вывести фильм в определенном жанре
```

**Задание 3. CREATE**

Реализовать следующие функции
```python
def add_review():
    # Добавление отзыва

def add_permissions_for_role():
    # Добавление права доступа для определенной роли
```

**Задание 4. UPDATE**

Реализовать следующие функции
```python
def update_user_email(user_id, new_email):
    # Обновление почты пользователя
```

**Дополнительное задание(по желанию)**:

Реализовать меню, через которое можно будет запускать все функции